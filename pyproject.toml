[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "the-matrix"
version = "0.1.0"
description = "A from-scratch matrix and Markov chain learning project."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
